// STUB — agent domain types (VibeProfile, FactResult, Fallacy, EvalScore,
// ShieldResult, SwordResult, Source) land in Phase 1 per doc/DATA_SCHEMAS.md §1.
// Re-export point for renderer + main once filled.

export {};
